# 📊 1. ERD (Entity Relationship Diagram)
___
 🏗️ Database Schema Visualization:
---
___

 🔗 Entity Relationship Diagram
---
___

```text
┌─────────────────────┐         ┌─────────────────────┐
│        USERS        │         │        POSTS        │
├─────────────────────┤         ├─────────────────────┤
│ id (PK)             │◄────────┤ user_id (FK)        │
│ email (UNIQUE)      │         │ id (PK)             │
│ password            │         │ content             │
│ username (UNIQUE)   │         │ hashtags            │
│ user_type           │         │ is_promotional      │
│ name                │         │ created_at          │
│ bio                 │         │ updated_at          │
│ is_private          │         └─────────┬───────────┘
│ is_public           │                   │
│ security_question   │                   │
│ security_answer     │                   │
│ password_hint       │                   │
│ created_at          │                   ▼
└──────────┬──────────┘         ┌─────────────────────┐
           │                    │      COMMENTS       │
           ▼                    ├─────────────────────┤
┌─────────────────────┐         │ id (PK)             │
│    CONNECTIONS      │◄────────┤ post_id (FK)        │
├─────────────────────┤         │ user_id (FK)        │
│ id (PK)             │         │ content             │
│ requester_id (FK)   │         │ created_at          │
│ receiver_id (FK)    │         └─────────┬───────────┘
│ status              │                   │
│ is_connected        │                   │
│ created_at          │                   ▼
└──────────┬──────────┘         ┌─────────────────────┐
           │                    │        LIKES        │
           ▼                    ├─────────────────────┤
┌─────────────────────┐         │ id (PK)             │
│       FOLLOWS       │◄────────┤ post_id (FK)        │
├─────────────────────┤         │ user_id (FK)        │
│ id (PK)             │         │ created_at          │
│ follower_id (FK)    │         └─────────────────────┘
│ followed_id (FK)    │
│ status              │
│ created_at          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│      REPOSTS        │
├─────────────────────┤
│ id (PK)             │
│ original_post_id(FK)│
│ user_id (FK)        │
│ created_at          │
└─────────────────────┘

           │
           ▼
┌─────────────────────┐
│   NOTIFICATIONS     │
├─────────────────────┤
│ id (PK)             │
│ user_id (FK)        │
│ type                │
│ message             │
│ is_read             │
│ created_at          │
└─────────────────────┘

```
___
## 🔑 Relationships Summary

```

┌────────────────────────────────────┬────────┬─────────────────────────────────────────────────┐
│        RELATIONSHIP                │  TYPE  │                DESCRIPTION                      │
├────────────────────────────────────┼────────┼─────────────────────────────────────────────────┤
│ Users → Posts                      │  1:N   │ A user can create multiple posts                │
│ Users → Comments                   │  1:N   │ A user can write multiple comments              │
│ Users → Likes                      │  1:N   │ A user can like multiple posts                  │
│ Users → Reposts                    │  1:N   │ A user can repost multiple posts                │
│ Posts → Comments                   │  1:N   │ A post can have multiple comments               │
│ Posts → Likes                      │  1:N   │ A post can be liked by multiple users           │
│ Posts → Reposts                    │  1:N   │ A post can be reposted by multiple users        │
│ Users → Connections (as requester) │  1:N   │ A user can send multiple connection requests    │
│ Users → Connections (as receiver)  │  1:N   │ A user can receive multiple connection requests │
│ Users → Follows (as follower)      │  1:N   │ A user can follow multiple users                │
│ Users → Follows (as followed)      │  1:N   │ A user can be followed by multiple users        │
│ Users → Notifications              │  1:N   │ A user can have multiple notifications          │
│ Users → Users (Connections)        │  M:N   │ Users can connect with each other               │
│ Users → Users (Follows)            │  M:N   │ Users can follow each other                     │
└────────────────────────────────────┴────────┴─────────────────────────────────────────────────┘
```